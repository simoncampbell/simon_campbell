define(['domReady',
        'jquery',
        'widgets'],
    function(domReady,
            jQuery) {

        var App = window.App || {},
            runlist,
            sels;

        window.App = App;

        return App;
    }
);